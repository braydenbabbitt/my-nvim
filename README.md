# Brayden's Neovim Config

This is a WIP custom config started from `kickstart.nvim` to replace my current usage of the LazyVim distro.

This configuration requires some dependencies:
- gcc
- lazygit
- npm
